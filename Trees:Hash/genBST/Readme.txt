To compile this program, you must include the flag -fpermissive in your compile options.

In Dev-C++ this can be set under Tools > Compiler Options. 

Add -fpermissive in the textbox titled "Add the following commands when calling compiler". Be sure to select the checkbox for this next to this option.
